#include<bits/stdc++.h>
#include<algorithm>
using namespace std;
#define ll long long
#define endl '\n'
#define cy cout << "YES\n"
#define cn cout << "NO\n"
#define nl "\n"
#define ar(v) v.begin(), v.end()
#define rar(v) v.rbegin(), v.rend()
#define fl(k,g) for(ll i=k;i<g;i++)
#define fl2(o,p) for(ll j=o;j<=p;j++)
#define pb   push_back
#define pob  pop_back
int main()
{  ios_base::sync_with_stdio(false);
   cin.tie(nullptr);
   ll n;   cin>>n;
   while(n--){
     string s;    cin>>s;
     ll y=count(s.begin(),s.end(),'Y');
     ll t=count(s.begin(),s.end(),'N');
     //bool f=0;
 
    // fl(0,s.size()){
    //  if(s[i]=='Y' && s[i+1]=='Y'){
       // f=1;
    //  break;
     // }
    // }
 
   if(y<=1)cy;
  // else if(y>t+1)cn;
    else cn;
   }
}
